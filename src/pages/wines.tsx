import type { NextPage } from "next";
// import가 뭔가요? > 몰라도됨
const WinePage: NextPage = () => {
  // NextPage는 템플릿 선언같은 것 ?
  return (
    <div>
      <h1>Wine</h1>
      Wine
    </div>
  );
};

export default WinePage;
// export가 뭔가요? > 몰라도 됨
// 가장기본적인 템플릿, TypeScript를 쓰는
